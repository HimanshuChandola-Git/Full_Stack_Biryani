'use client'

import { useMemo, useState } from 'react'
import { AreaChart, Area, BarChart, Bar, CartesianGrid, Cell, LineChart, Line, PieChart, Pie, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import { Activity, ArrowRight, BarChart3, BookOpen, Box, Calculator, Check, ChevronRight, CircleHelp, ClipboardList, CloudSun, Compass, Download, FlaskConical, Globe2, Layers3, Menu, PanelLeftClose, Play, Plus, Search, Settings2, SlidersHorizontal, Sparkles, Thermometer, TrendingDown, Wind, X } from 'lucide-react'
import { climates, defaultDesign, materialById, materials, climateById, optimizeShelter, simulateShelter, type ShelterDesign } from '@/lib/thermoshelter'
import { repository } from '@/lib/supabase-repository'

type Section='Dashboard'|'Climate'|'Shelter Design'|'Materials'|'Simulation'|'Results'|'Compare Designs'|'Optimization'|'Reports'
const nav: {label:Section; icon: typeof Activity}[]=[{label:'Dashboard',icon:Activity},{label:'Climate',icon:CloudSun},{label:'Shelter Design',icon:Box},{label:'Materials',icon:Layers3},{label:'Simulation',icon:FlaskConical},{label:'Results',icon:BarChart3},{label:'Compare Designs',icon:Compass},{label:'Optimization',icon:Sparkles},{label:'Reports',icon:ClipboardList}]
const stages=['Climate','Shelter Design','Materials','Simulation','Results','Optimization','Reports']

export default function Page(){
 const [section,setSection]=useState<Section>('Dashboard'); const [mobileNav,setMobileNav]=useState(false); const [climateId,setClimateId]=useState('ladakh'); const [design,setDesign]=useState<ShelterDesign>(defaultDesign); const [materialsSearch,setMaterialsSearch]=useState(''); const [materialCategory,setMaterialCategory]=useState('all'); const [simulationRunning,setSimulationRunning]=useState(false); const [simulationDone,setSimulationDone]=useState(false); const [optimizationRunning,setOptimizationRunning]=useState(false); const [optimized,setOptimized]=useState(false); const [saved,setSaved]=useState(false)
 const climate=climateById(climateId); const sim=useMemo(()=>simulateShelter(design,climate),[design,climate]); const opt=useMemo(()=>optimizeShelter({},climate),[climate]); const currentStage=stages.indexOf(section)
 const update=(key:keyof ShelterDesign,value:string|number)=>setDesign(prev=>({...prev,[key]:value}))
 const go=(s:Section)=>{setSection(s);setMobileNav(false)}
 const runSimulation=()=>{setSimulationRunning(true);setSimulationDone(false);setTimeout(()=>{setSimulationRunning(false);setSimulationDone(true);go('Results'); repository.saveSimulation(design.id,sim)},1200)}
 const runOptimization=()=>{setOptimizationRunning(true);setTimeout(()=>{setOptimizationRunning(false);setOptimized(true);repository.saveOptimization(climate.id,opt)},1100)}
 const filtered=materials.filter(m=>(materialCategory==='all'||m.category===materialCategory)&&m.name.toLowerCase().includes(materialsSearch.toLowerCase()))
 return <div className="min-h-screen bg-background text-foreground"><aside className={`fixed inset-y-0 left-0 z-40 flex w-64 flex-col border-r border-border bg-sidebar transition-transform lg:translate-x-0 ${mobileNav?'translate-x-0':'-translate-x-full'}`}><div className="flex h-20 items-center gap-3 border-b border-sidebar-border px-6"><div className="grid size-9 place-items-center rounded-lg bg-primary text-primary-foreground"><Thermometer className="size-5"/></div><div><div className="font-semibold tracking-tight">ThermoShelter</div><div className="text-[10px] uppercase tracking-[0.18em] text-muted-foreground">SIH26051 / PROTOTYPE</div></div><button className="ml-auto lg:hidden" onClick={()=>setMobileNav(false)} aria-label="Close navigation"><X className="size-4"/></button></div><div className="flex-1 px-3 py-5"><div className="mb-3 px-3 text-[10px] font-semibold uppercase tracking-[0.16em] text-muted-foreground">Workspace</div><nav className="flex flex-col gap-1">{nav.map(item=>{const Icon=item.icon;return <button key={item.label} onClick={()=>go(item.label)} className={`flex items-center gap-3 rounded-md px-3 py-2.5 text-sm transition-colors ${section===item.label?'bg-sidebar-primary text-sidebar-primary-foreground':'text-sidebar-foreground hover:bg-sidebar-accent'}`}><Icon className="size-4"/><span>{item.label}</span>{item.label==='Results'&&simulationDone?<span className="ml-auto size-1.5 rounded-full bg-primary"/>:null}</button>})}</nav></div><div className="border-t border-sidebar-border p-4"><div className="rounded-lg bg-sidebar-accent p-3"><div className="flex items-center gap-2 text-xs font-medium"><CircleHelp className="size-3.5 text-primary"/>Prototype mode</div><p className="mt-2 text-[11px] leading-relaxed text-muted-foreground">Outputs are clearly labeled demo data and are not validated physics results.</p></div></div></aside><div className="lg:pl-64"><header className="sticky top-0 z-30 flex h-16 items-center justify-between border-b border-border bg-background/95 px-4 backdrop-blur lg:px-8"><div className="flex items-center gap-3"><button className="lg:hidden" onClick={()=>setMobileNav(true)} aria-label="Open navigation"><Menu className="size-5"/></button><div className="hidden items-center gap-2 text-sm sm:flex"><span className="text-muted-foreground">Project</span><ChevronRight className="size-3 text-muted-foreground"/><span className="font-medium">Ladakh Passive Study</span></div></div><div className="flex items-center gap-3"><div className="hidden items-center gap-2 rounded-full border border-border px-3 py-1.5 text-xs sm:flex"><span className="size-2 rounded-full bg-emerald-500"/>Simulation ready</div><button className="grid size-8 place-items-center rounded-full bg-muted text-xs font-semibold">AK</button></div></header><main className="mx-auto max-w-[1440px] px-4 py-6 lg:px-8"><div className="mb-7 flex items-center justify-between gap-4"><div><div className="mb-2 flex items-center gap-2 text-xs text-muted-foreground"><span>Workspace</span><ChevronRight className="size-3"/><span className="text-foreground">{section}</span></div><h1 className="text-2xl font-semibold tracking-tight">{section==='Dashboard'?'Design shelters that work with the climate, not against it.':section}</h1><p className="mt-1 max-w-2xl text-sm text-muted-foreground">{section==='Dashboard'?'Area-specific thermal simulation and optimization for energy-efficient passive shelters.':'Configure, evaluate, and explain a climate-aware shelter design.'}</p></div>{section==='Dashboard'&&<button onClick={()=>go('Climate')} className="hidden items-center gap-2 rounded-md bg-primary px-4 py-2.5 text-sm font-medium text-primary-foreground shadow-sm sm:flex">Start designing <ArrowRight className="size-4"/></button>}</div><div className="mb-7 flex items-center gap-1 overflow-x-auto pb-1">{stages.map((stage,i)=><div key={stage} className="flex shrink-0 items-center gap-1"><button onClick={()=>go(stage as Section)} className={`flex items-center gap-2 rounded-full px-3 py-1.5 text-xs font-medium ${section===stage?'bg-primary text-primary-foreground':'bg-muted text-muted-foreground'}`}><span className="grid size-4 place-items-center rounded-full bg-background/20 text-[10px]">{i+1}</span>{stage}</button>{i<stages.length-1&&<ChevronRight className="size-3 text-muted-foreground"/>}</div>)}</div>{section==='Dashboard'?<Dashboard go={go} climate={climate} design={design} sim={sim}/>:section==='Climate'?<ClimatePage climateId={climateId} setClimateId={setClimateId} go={go}/>:section==='Shelter Design'?<DesignPage design={design} update={update} onSave={()=>{setSaved(true);repository.saveShelterDesign(design)}} go={go} saved={saved}/>:section==='Materials'?<MaterialsPage filtered={filtered} search={materialsSearch} setSearch={setMaterialsSearch} category={materialCategory} setCategory={setMaterialCategory} design={design} update={update} go={go}/>:section==='Simulation'?<SimulationPage design={design} climate={climate} run={runSimulation} running={simulationRunning} done={simulationDone}/>:section==='Results'?<ResultsPage sim={sim} design={design} go={go}/>:section==='Compare Designs'?<ComparePage sim={sim}/>:section==='Optimization'?<OptimizationPage opt={opt} running={optimizationRunning} optimized={optimized} run={runOptimization}/>:<ReportsPage design={design} climate={climate} sim={sim}/>}</main></div></div>
}
function Dashboard({go,climate,design,sim}:{go:(s:Section)=>void;climate:ReturnType<typeof climateById>;design:ShelterDesign;sim:ReturnType<typeof simulateShelter>}){return <div className="grid gap-5 lg:grid-cols-[1.3fr_0.7fr]"><section className="rounded-xl border border-border bg-card p-6 lg:p-8"><div className="mb-8 flex items-start justify-between"><div><div className="mb-3 inline-flex items-center gap-2 rounded-full border border-border px-2.5 py-1 text-[11px] font-medium text-muted-foreground"><span className="size-1.5 rounded-full bg-primary"/>Area-specific design workspace</div><h2 className="max-w-xl text-3xl font-semibold leading-tight tracking-tight lg:text-4xl">Passive shelter design, grounded in local climate.</h2><p className="mt-4 max-w-lg text-sm leading-6 text-muted-foreground">Translate climate conditions into design decisions, compare material assemblies, and understand why an option performs better.</p></div><div className="hidden rounded-lg border border-border bg-muted p-3 md:block"><Compass className="size-5 text-primary"/></div></div><div className="flex flex-wrap gap-3"><button onClick={()=>go('Climate')} className="inline-flex items-center gap-2 rounded-md bg-primary px-4 py-2.5 text-sm font-medium text-primary-foreground">Start designing <ArrowRight className="size-4"/></button><button onClick={()=>go('Simulation')} className="inline-flex items-center gap-2 rounded-md border border-border px-4 py-2.5 text-sm font-medium">Explore workflow <Play className="size-4"/></button></div></section><section className="rounded-xl border border-border bg-primary p-6 text-primary-foreground"><div className="flex items-center justify-between"><div className="text-xs font-medium uppercase tracking-[0.14em] opacity-70">Current project</div><Settings2 className="size-4 opacity-70"/></div><div className="mt-8 text-lg font-semibold">{design.name}</div><div className="mt-4 grid grid-cols-2 gap-4 text-sm"><div><div className="text-xs opacity-60">Location</div><div className="mt-1">{climate.location}</div></div><div><div className="text-xs opacity-60">Geometry</div><div className="mt-1">{design.length} × {design.width} × {design.height} m</div></div><div><div className="text-xs opacity-60">Climate</div><div className="mt-1">{climate.climateType}</div></div><div><div className="text-xs opacity-60">Status</div><div className="mt-1 flex items-center gap-1.5"><span className="size-1.5 rounded-full bg-emerald-300"/>Ready to simulate</div></div></div></section><div className="grid gap-4 sm:grid-cols-3 lg:col-span-2"><Feature icon={SunIcon} title="Solar optimization" text="Maximize useful solar gain."/><Feature icon={Thermometer} title="Thermal simulation" text="Predict temperature and heat flow."/><Feature icon={Sparkles} title="Design optimization" text="Find an efficient configuration."/></div><section className="rounded-xl border border-border bg-card lg:col-span-2"><div className="flex items-center justify-between border-b border-border p-5"><div><h3 className="font-semibold">Thermal outlook</h3><p className="text-xs text-muted-foreground">Predicted demo output for the current design</p></div><button onClick={()=>go('Results')} className="text-xs font-medium text-primary">View results <ArrowRight className="ml-1 inline size-3"/></button></div><div className="grid gap-4 p-5 sm:grid-cols-3"><Metric label="Avg. internal temp" value={`${sim.averageInternalTemperature} °C`} icon={<Thermometer/>}/><Metric label="Useful solar gain" value={`${(sim.totalSolarGain/1000).toFixed(1)} kWh`} icon={<TrendingDown/>}/><Metric label="Heat loss" value={`${(sim.totalHeatLoss/1000).toFixed(1)} kWh`} icon={<Wind/>}/></div></section></div>}
function Feature({icon:Icon,title,text}:{icon:typeof Activity;title:string;text:string}){return <div className="rounded-xl border border-border bg-card p-5"><div className="mb-5 grid size-9 place-items-center rounded-md bg-muted text-primary"><Icon className="size-4"/></div><h3 className="text-sm font-semibold">{title}</h3><p className="mt-1 text-xs leading-5 text-muted-foreground">{text}</p></div>}
function Metric({label,value,icon}:{label:string;value:string;icon:React.ReactNode}){return <div className="rounded-lg bg-muted p-4"><div className="flex items-center justify-between text-muted-foreground"><span className="text-xs">{label}</span><span className="[&>svg]:size-4">{icon}</span></div><div className="mt-3 text-2xl font-semibold tracking-tight">{value}</div><div className="mt-1 text-[10px] uppercase tracking-wider text-muted-foreground">Predicted · Demo data</div></div>}
function SunIcon(){return <CloudSun className="size-4"/>}
function ClimatePage({climateId,setClimateId,go}:{climateId:string;setClimateId:(id:string)=>void;go:(s:Section)=>void}){const c=climateById(climateId);return <div className="grid gap-5 lg:grid-cols-[1fr_0.65fr]"><section className="rounded-xl border border-border bg-card p-6"><div className="mb-6 flex items-start justify-between"><div><h2 className="font-semibold">Climate profile</h2><p className="mt-1 text-xs text-muted-foreground">Choose a preset or prepare custom conditions.</p></div><button className="rounded-md border border-border p-2" aria-label="Use location data"><Globe2 className="size-4"/></button></div><div className="mb-6 grid grid-cols-3 gap-2">{climates.map(item=><button key={item.id} onClick={()=>setClimateId(item.id)} className={`rounded-lg border p-3 text-left ${climateId===item.id?'border-primary bg-primary/5':'border-border'}`}><div className="text-xs font-medium">{item.name}</div><div className="mt-1 text-[10px] text-muted-foreground">{item.climateType}</div></button>)}</div><div className="grid gap-4 sm:grid-cols-2">{[['Location',c.location],['Climate type',c.climateType],['Ambient temperature',`${c.ambientTemperature} °C`],['Solar radiation',`${c.solarRadiation} W/m²`],['Sunshine duration',`${c.sunshineDuration} hours/day`],['Wind speed',`${c.windSpeed} m/s`],['Relative humidity',`${c.relativeHumidity} %`],['Simulation duration','24 hours']].map(([label,value])=><label key={label} className="grid gap-2 text-xs font-medium">{label}<input value={value} readOnly className="h-10 rounded-md border border-input bg-background px-3 text-sm font-normal outline-none"/></label>)}</div><div className="mt-6 flex flex-wrap gap-3"><button onClick={()=>go('Shelter Design')} className="rounded-md bg-primary px-4 py-2.5 text-sm font-medium text-primary-foreground">Save climate profile</button><button className="rounded-md border border-border px-4 py-2.5 text-sm font-medium"><Plus className="mr-2 inline size-4"/>Custom profile</button></div></section><InfoPanel title="Why climate matters" icon={<CloudSun/>}><p>Local climate sets the boundary conditions for thermal comfort. Solar radiation, wind, humidity, and ambient temperature influence the shelter strategy.</p><div className="mt-5 grid grid-cols-2 gap-3"><Metric label="Solar resource" value={`${c.solarRadiation} W/m²`} icon={<SunIcon/>}/><Metric label="Ambient baseline" value={`${c.ambientTemperature} °C`} icon={<Thermometer/>}/></div></InfoPanel></div>}
function InfoPanel({title,icon,children}:{title:string;icon:React.ReactNode;children:React.ReactNode}){return <section className="rounded-xl border border-border bg-card p-6"><div className="flex items-center gap-2 font-semibold"><span className="text-primary [&>svg]:size-4">{icon}</span>{title}</div><div className="mt-4 text-sm leading-6 text-muted-foreground">{children}</div></section>}
function DesignPage({design,update,onSave,go,saved}:{design:ShelterDesign;update:(key:keyof ShelterDesign,value:string|number)=>void;onSave:()=>void;go:(s:Section)=>void;saved:boolean}){return <div className="grid gap-5 lg:grid-cols-[1fr_0.8fr]"><section className="rounded-xl border border-border bg-card p-6"><div className="mb-6"><h2 className="font-semibold">Shelter geometry</h2><p className="mt-1 text-xs text-muted-foreground">Set dimensions and openings for the first design iteration.</p></div><div className="grid gap-4 sm:grid-cols-3">{([['length','Length','m'],['width','Width','m'],['height','Height','m'],['windowAreaPercentage','Window area','%'],['doorArea','Door area','m²'],['numberOfWindows','Windows','count']] as const).map(([key,label,unit])=><label key={key} className="grid gap-2 text-xs font-medium">{label} <div className="relative"><input type="number" min="0" value={design[key]} onChange={e=>update(key,Number(e.target.value))} className="h-10 w-full rounded-md border border-input bg-background px-3 pr-12 text-sm outline-none"/><span className="absolute right-3 top-3 text-[10px] text-muted-foreground">{unit}</span></div></label>)}</div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="grid gap-2 text-xs font-medium">Shape<select value={design.shape} onChange={e=>update('shape',e.target.value)} className="h-10 rounded-md border border-input bg-background px-3 text-sm"><option>Rectangular</option><option>Square</option></select></label><label className="grid gap-2 text-xs font-medium">Orientation<select value={design.orientation} onChange={e=>update('orientation',e.target.value)} className="h-10 rounded-md border border-input bg-background px-3 text-sm"><option>North</option><option>South</option><option>East</option><option>West</option></select></label></div><div className="mt-6 flex flex-wrap gap-3"><button onClick={onSave} className="rounded-md bg-primary px-4 py-2.5 text-sm font-medium text-primary-foreground">{saved?'Design saved':'Save design'}</button><button onClick={()=>go('Materials')} className="rounded-md border border-border px-4 py-2.5 text-sm font-medium">Continue to materials <ArrowRight className="ml-1 inline size-4"/></button></div></section><DesignPreview design={design}/></div>}
function DesignPreview({design}:{design:ShelterDesign}){return <section className="rounded-xl border border-border bg-card p-6"><div className="mb-5 flex items-center justify-between"><div><h2 className="font-semibold">Engineering preview</h2><p className="mt-1 text-xs text-muted-foreground">Conceptual 2D view · updates live</p></div><Badge>DEMO</Badge></div><div className="relative flex min-h-72 items-center justify-center overflow-hidden rounded-lg bg-muted"><div className="absolute inset-x-8 bottom-10 border-b border-dashed border-primary/40"/><div className="relative h-40 w-64 border-2 border-primary/70 bg-card" style={{transform:`perspective(500px) rotateX(3deg) rotateY(${design.orientation==='East'?'-8deg':design.orientation==='West'?'8deg':'0deg'})`}}><div className="absolute -top-10 left-1/2 h-10 w-[calc(100%+2px)] -translate-x-1/2 skew-x-[-25deg] border-2 border-b-0 border-primary/70 bg-primary/10"/><div className="absolute left-7 top-16 h-12 w-12 border-2 border-sky-700/60 bg-sky-200/40"/><div className="absolute right-7 top-14 h-16 w-9 border-2 border-primary/60 bg-primary/10"/><div className="absolute bottom-0 left-1/2 h-20 w-12 -translate-x-1/2 border-2 border-b-0 border-primary/60 bg-muted"/></div><div className="absolute bottom-3 left-4 text-[10px] text-muted-foreground">{design.length}m</div><div className="absolute left-3 top-4 text-[10px] text-muted-foreground">N ↑</div></div><div className="mt-5 grid grid-cols-3 gap-3 text-center"><div><div className="text-lg font-semibold">{design.length*design.width}</div><div className="text-[10px] uppercase text-muted-foreground">Floor area m²</div></div><div><div className="text-lg font-semibold">{design.windowAreaPercentage}%</div><div className="text-[10px] uppercase text-muted-foreground">Openings</div></div><div><div className="text-lg font-semibold">{design.orientation}</div><div className="text-[10px] uppercase text-muted-foreground">Orientation</div></div></div></section>}
function Badge({children}:{children:React.ReactNode}){return <span className="rounded-full border border-primary/30 bg-primary/10 px-2 py-1 text-[10px] font-semibold tracking-wider text-primary">{children}</span>}
function MaterialsPage({filtered,search,setSearch,category,setCategory,design,update,go}:{filtered:typeof materials;search:string;setSearch:(s:string)=>void;category:string;setCategory:(s:string)=>void;design:ShelterDesign;update:(key:keyof ShelterDesign,value:string|number)=>void;go:(s:Section)=>void}){const picks:[keyof ShelterDesign,string][]=[['wallMaterialId','Wall'],['roofMaterialId','Roof'],['floorMaterialId','Floor'],['windowMaterialId','Window'],['doorMaterialId','Door'],['thermalStorageMaterialId','Storage']];return <div className="grid gap-5 lg:grid-cols-[0.7fr_1.3fr]"><section className="rounded-xl border border-border bg-card p-6"><div className="mb-5 flex items-center justify-between"><div><h2 className="font-semibold">Selected assembly</h2><p className="mt-1 text-xs text-muted-foreground">Choose a material for each component.</p></div><Badge>DEMO DATA</Badge></div><div className="flex flex-col gap-2">{picks.map(([key,label])=>{const m=materialById(String(design[key]));return <button key={key} onClick={()=>{const first=filtered.find(x=>x.category===(key==='wallMaterialId'?'wall':key==='roofMaterialId'?'roof':key==='floorMaterialId'?'floor':key==='windowMaterialId'?'window':key==='doorMaterialId'?'door':'thermal_storage'));if(first)update(key,first.id)}} className="flex items-center justify-between rounded-lg border border-border p-3 text-left hover:border-primary"><div><div className="text-xs font-medium">{label}</div><div className="mt-1 text-sm">{m.name}</div></div><div className="text-right text-[10px] text-muted-foreground"><div>R {m.thermalResistance} m²K/W</div><div>k {m.thermalConductivity} W/mK</div></div></button>})}</div><button onClick={()=>go('Simulation')} className="mt-5 w-full rounded-md bg-primary px-4 py-2.5 text-sm font-medium text-primary-foreground">Continue to simulation <ArrowRight className="ml-1 inline size-4"/></button></section><section className="rounded-xl border border-border bg-card p-6"><div className="mb-5 flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-semibold">Material library</h2><p className="mt-1 text-xs text-muted-foreground">Example values are placeholders until validated.</p></div><div className="flex gap-2"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-muted-foreground"/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search materials" className="h-10 w-48 rounded-md border border-input bg-background pl-9 pr-3 text-sm"/></div><select value={category} onChange={e=>setCategory(e.target.value)} className="h-10 rounded-md border border-input bg-background px-2 text-xs"><option value="all">All categories</option><option value="wall">Wall</option><option value="roof">Roof</option><option value="floor">Floor</option><option value="window">Window</option><option value="door">Door</option><option value="thermal_storage">Storage</option></select></div></div><div className="grid gap-3 sm:grid-cols-2">{filtered.map(m=><article key={m.id} className="rounded-lg border border-border p-4"><div className="flex items-start justify-between"><div><div className="text-[10px] uppercase tracking-wider text-muted-foreground">{m.category.replace('_',' ')}</div><h3 className="mt-1 text-sm font-semibold">{m.name}</h3></div><button className="text-muted-foreground" aria-label={`Compare ${m.name}`}><SlidersHorizontal className="size-4"/></button></div><p className="mt-2 text-xs leading-5 text-muted-foreground">{m.description}</p><div className="mt-4 grid grid-cols-3 gap-2 border-t border-border pt-3 text-[10px] text-muted-foreground"><div><b className="block text-xs text-foreground">{m.thermalConductivity}</b>W/mK</div><div><b className="block text-xs text-foreground">{m.density}</b>kg/m³</div><div><b className="block text-xs text-foreground">{m.defaultThickness}</b>mm</div></div></article>)}</div></section></div>}
function SimulationPage({design,climate,run,running,done}:{design:ShelterDesign;climate:ReturnType<typeof climateById>;run:()=>void;running:boolean;done:boolean}){return <div className="grid gap-5 lg:grid-cols-[1fr_0.7fr]"><section className="rounded-xl border border-border bg-card p-6"><div className="mb-6"><h2 className="font-semibold">Simulation configuration</h2><p className="mt-1 text-xs text-muted-foreground">Review inputs before running the prototype engine.</p></div><div className="grid gap-3 sm:grid-cols-2">{[['Climate',`${climate.location} · ${climate.climateType}`],['Geometry',`${design.shape} · ${design.length} × ${design.width} × ${design.height} m`],['Orientation',design.orientation],['Openings',`${design.windowAreaPercentage}% windows · ${design.numberOfWindows} openings`],['Wall material',materialById(design.wallMaterialId).name],['Roof material',materialById(design.roofMaterialId).name]].map(([a,b])=><div key={a} className="rounded-lg bg-muted p-4"><div className="text-[10px] uppercase tracking-wider text-muted-foreground">{a}</div><div className="mt-2 text-sm font-medium">{b}</div></div>)}</div><button disabled={running} onClick={run} className="mt-6 flex items-center gap-2 rounded-md bg-primary px-5 py-3 text-sm font-medium text-primary-foreground disabled:opacity-60"><Play className="size-4"/>{running?'Running demo simulation…':'Run simulation'}</button>{done&&<div className="mt-4 flex items-center gap-2 text-sm text-emerald-700"><Check className="size-4"/>Simulation output is ready.</div>}</section><section className="rounded-xl border border-border bg-card p-6"><div className="flex items-center justify-between"><h2 className="font-semibold">Engine pipeline</h2><Badge>MOCK SERVICE</Badge></div><div className="mt-6 flex flex-col gap-4">{['Preparing climate data','Calculating solar gain','Calculating heat transfer','Calculating thermal losses','Generating results'].map((x,i)=><div key={x} className="flex items-center gap-3"><span className={`grid size-7 place-items-center rounded-full text-xs ${running&&i<4?'bg-primary text-primary-foreground':'bg-muted text-muted-foreground'}`}>{running&&i<4?<Activity className="size-3.5"/>:i+1}</span><span className="text-sm text-muted-foreground">{x}</span></div>)}</div><p className="mt-8 border-t border-border pt-4 text-xs leading-5 text-muted-foreground">This frontend result is generated by a replaceable mock service. A future integration can call FastAPI and a Python thermal engine using the same structured output contract.</p></section></div>}
function ResultsPage({sim,design,go}:{sim:ReturnType<typeof simulateShelter>;design:ShelterDesign;go:(s:Section)=>void}){return <div className="flex flex-col gap-5"><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Internal temperature" value={`${sim.averageInternalTemperature} °C`} icon={<Thermometer/>}/><Metric label="Solar energy gain" value={`${(sim.totalSolarGain/1000).toFixed(1)} kWh`} icon={<SunIcon/>}/><Metric label="Total heat loss" value={`${(sim.totalHeatLoss/1000).toFixed(1)} kWh`} icon={<Wind/>}/><Metric label="Heating requirement" value={`${(sim.totalHeatingRequirement/1000).toFixed(1)} kWh`} icon={<Activity/>}/></div><div className="grid gap-5 lg:grid-cols-[1.4fr_0.6fr]"><ChartCard title="Internal temperature vs ambient" subtitle="Predicted · 24 hour demo output"><ResponsiveContainer width="100%" height={260}><LineChart data={sim.points}><CartesianGrid strokeDasharray="3 3" vertical={false} stroke="var(--border)"/><XAxis dataKey="timestamp" tick={{fontSize:10}} interval={3}/><YAxis tick={{fontSize:10}}/><Tooltip/><Line type="monotone" dataKey="internalTemperature" stroke="var(--primary)" strokeWidth={2} dot={false} name="Internal °C"/><Line type="monotone" dataKey="ambientTemperature" stroke="var(--muted-foreground)" strokeWidth={1.5} dot={false} name="Ambient °C"/></LineChart></ResponsiveContainer></ChartCard><ChartCard title="Heat loss breakdown" subtitle="Relative distribution · demo"><ResponsiveContainer width="100%" height={260}><PieChart><Pie data={sim.breakdown} dataKey="value" nameKey="name" innerRadius={65} outerRadius={92} paddingAngle={3}>{sim.breakdown.map(x=><Cell key={x.name} fill={x.color}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer><div className="grid grid-cols-2 gap-2">{sim.breakdown.map(x=><div key={x.name} className="flex items-center gap-2 text-xs"><span className="size-2 rounded-full" style={{background:x.color}}/>{x.name}<span className="ml-auto text-muted-foreground">{x.value}%</span></div>)}</div></ChartCard></div><div className="grid gap-5 lg:grid-cols-2"><ChartCard title="Solar radiation and gain" subtitle="W/m² and relative useful gain"><ResponsiveContainer width="100%" height={220}><AreaChart data={sim.points}><CartesianGrid strokeDasharray="3 3" vertical={false} stroke="var(--border)"/><XAxis dataKey="timestamp" tick={{fontSize:10}} interval={3}/><YAxis tick={{fontSize:10}}/><Tooltip/><Area type="monotone" dataKey="solarRadiation" fill="var(--chart-2)" fillOpacity={0.18} stroke="var(--chart-2)" name="Solar W/m²"/><Area type="monotone" dataKey="solarGain" fill="var(--chart-1)" fillOpacity={0.2} stroke="var(--chart-1)" name="Gain W"/></AreaChart></ResponsiveContainer></ChartCard><ChartCard title="Heat loss and heating requirement" subtitle="Hourly predicted values"><ResponsiveContainer width="100%" height={220}><BarChart data={sim.points}><CartesianGrid strokeDasharray="3 3" vertical={false} stroke="var(--border)"/><XAxis dataKey="timestamp" tick={{fontSize:10}} interval={3}/><YAxis tick={{fontSize:10}}/><Tooltip/><Bar dataKey="heatLoss" fill="var(--chart-2)" name="Heat loss W" radius={[3,3,0,0]}/><Bar dataKey="heatingRequirement" fill="var(--chart-1)" name="Heating W" radius={[3,3,0,0]}/></BarChart></ResponsiveContainer></ChartCard></div><div className="flex flex-wrap items-center justify-between rounded-xl border border-border bg-card p-5"><div><div className="text-xs uppercase tracking-wider text-muted-foreground">Design summary</div><div className="mt-2 text-sm font-medium">{design.name} · {design.orientation} orientation · {design.length*design.width} m² floor area</div></div><button onClick={()=>go('Compare Designs')} className="mt-3 rounded-md border border-border px-4 py-2 text-sm font-medium sm:mt-0">Compare designs <ArrowRight className="ml-1 inline size-4"/></button></div></div>}
function ChartCard({title,subtitle,children}:{title:string;subtitle:string;children:React.ReactNode}){return <section className="rounded-xl border border-border bg-card p-5"><div className="mb-4"><h3 className="text-sm font-semibold">{title}</h3><p className="mt-1 text-xs text-muted-foreground">{subtitle}</p></div>{children}</section>}
function ComparePage({sim}:{sim:ReturnType<typeof simulateShelter>}){return <div className="flex flex-col gap-5"><section className="rounded-xl border border-border bg-card p-6"><div className="flex items-center justify-between"><div><h2 className="font-semibold">Compare saved configurations</h2><p className="mt-1 text-xs text-muted-foreground">Relative comparison for the selected climate · {climateById('ladakh').name}</p></div><button className="rounded-md border border-border px-3 py-2 text-xs font-medium"><Plus className="mr-1 inline size-3"/>Add design</button></div><div className="mt-6 overflow-x-auto"><table className="w-full min-w-[760px] text-left text-xs"><thead className="border-b border-border text-muted-foreground"><tr>{['Design','Wall','Roof','Orientation','Window area','Avg. internal','Solar gain','Heat loss','Heating'].map(x=><th key={x} className="pb-3 pr-4 font-medium">{x}</th>)}</tr></thead><tbody><tr className="border-b border-border bg-primary/5"><td className="py-4 pr-4 font-semibold">Ladakh Passive Study <Badge>ACTIVE</Badge></td><td className="pr-4">Rammed Earth</td><td className="pr-4">Insulated Panel</td><td className="pr-4">South</td><td className="pr-4">12%</td><td className="pr-4 font-semibold">{sim.averageInternalTemperature} °C</td><td className="pr-4">{(sim.totalSolarGain/1000).toFixed(1)} kWh</td><td className="pr-4">{(sim.totalHeatLoss/1000).toFixed(1)} kWh</td><td>{(sim.totalHeatingRequirement/1000).toFixed(1)} kWh</td></tr><tr className="border-b border-border"><td className="py-4 pr-4 font-semibold">High Mass Variant</td><td className="pr-4">Compressed Earth</td><td className="pr-4">Mud + Lime</td><td className="pr-4">East</td><td className="pr-4">8%</td><td className="pr-4">15.2 °C</td><td className="pr-4">4.2 kWh</td><td className="pr-4">2.9 kWh</td><td>1.8 kWh</td></tr></tbody></table></div></section><ChartCard title="Relative performance" subtitle="Selected configurations · demo comparison"><ResponsiveContainer width="100%" height={280}><BarChart data={[{name:'Current',solar:sim.totalSolarGain/1000,loss:sim.totalHeatLoss/1000,heat:sim.totalHeatingRequirement/1000},{name:'High mass',solar:4.2,loss:2.9,heat:1.8}]}><CartesianGrid strokeDasharray="3 3" vertical={false} stroke="var(--border)"/><XAxis dataKey="name"/><YAxis/><Tooltip/><Bar dataKey="solar" fill="var(--chart-1)" name="Solar gain kWh"/><Bar dataKey="loss" fill="var(--chart-2)" name="Heat loss kWh"/><Bar dataKey="heat" fill="var(--chart-3)" name="Heating kWh"/></BarChart></ResponsiveContainer></ChartCard></div>}
function OptimizationPage({opt,running,optimized,run}:{opt:ReturnType<typeof optimizeShelter>;running:boolean;optimized:boolean;run:()=>void}){const [selected,setSelected]=useState(['Orientation','Wall Material','Roof Material','Window Area']);return <div className="grid gap-5 lg:grid-cols-[0.75fr_1.25fr]"><section className="rounded-xl border border-border bg-card p-6"><div className="mb-6"><h2 className="font-semibold">Optimization study</h2><p className="mt-1 text-xs leading-5 text-muted-foreground">Search the design space against a climate-specific objective.</p></div><div className="text-xs font-medium">Parameters to optimize</div><div className="mt-3 grid gap-2">{['Orientation','Wall Material','Roof Material','Window Area','Shelter Dimensions','Thermal Storage'].map(x=><label key={x} className="flex items-center gap-3 rounded-md border border-border p-3 text-sm"><input type="checkbox" checked={selected.includes(x)} onChange={()=>setSelected(s=>s.includes(x)?s.filter(y=>y!==x):[...s,x])} className="accent-primary"/>{x}</label>)}</div><div className="mt-6 text-xs font-medium">Objective</div><select className="mt-3 h-10 w-full rounded-md border border-input bg-background px-3 text-sm"><option>Maintain thermal comfort</option><option>Minimize heat loss</option><option>Maximize useful solar gain</option><option>Minimize heating requirement</option></select><button disabled={running} onClick={run} className="mt-6 w-full rounded-md bg-primary px-4 py-3 text-sm font-medium text-primary-foreground disabled:opacity-60">{running?'Searching possible configurations…':'Optimize design'} <Sparkles className="ml-1 inline size-4"/></button>{running&&<div className="mt-4 h-1.5 overflow-hidden rounded-full bg-muted"><div className="h-full w-2/3 animate-pulse rounded-full bg-primary"/></div>}</section><section className="rounded-xl border border-border bg-card p-6">{optimized?<><div className="flex items-center justify-between"><div><Badge>OPTIMAL DESIGN FOUND</Badge><h2 className="mt-3 text-2xl font-semibold tracking-tight">A stronger starting point for this climate.</h2></div><div className="text-right"><div className="text-3xl font-semibold text-primary">{opt.score}</div><div className="text-[10px] uppercase tracking-wider text-muted-foreground">Design score</div></div></div><div className="mt-7 grid gap-3 sm:grid-cols-2">{[['Orientation',opt.orientation],['Wall',opt.wall],['Roof',opt.roof],['Window area',`${opt.windowArea}%`],['Thermal storage',opt.thermalStorage],['Configurations evaluated','48']].map(([a,b])=><div key={a} className="rounded-lg bg-muted p-4"><div className="text-[10px] uppercase tracking-wider text-muted-foreground">{a}</div><div className="mt-2 text-sm font-semibold">{b}</div></div>)}</div><div className="mt-6 rounded-lg border border-primary/20 bg-primary/5 p-5"><div className="flex items-center gap-2 text-sm font-semibold"><Sparkles className="size-4 text-primary"/>Why this design?</div><div className="mt-4 grid gap-3">{opt.explanation.map(x=><div key={x} className="flex items-start gap-2 text-sm text-muted-foreground"><Check className="mt-0.5 size-4 shrink-0 text-primary"/>{x}</div>)}</div></div></>:<div className="flex min-h-96 flex-col items-center justify-center text-center"><div className="grid size-14 place-items-center rounded-full bg-muted text-primary"><Sparkles className="size-6"/></div><h2 className="mt-5 text-xl font-semibold">Find a better-fit configuration</h2><p className="mt-2 max-w-sm text-sm leading-6 text-muted-foreground">ThermoShelter will evaluate selected parameters relative to your climate and explain the recommendation.</p></div>}</section></div>}
function ReportsPage({design,climate,sim}:{design:ShelterDesign;climate:ReturnType<typeof climateById>;sim:ReturnType<typeof simulateShelter>}){return <div className="mx-auto max-w-4xl"><section className="rounded-xl border border-border bg-card p-8"><div className="flex flex-wrap items-start justify-between gap-4 border-b border-border pb-6"><div><div className="flex items-center gap-2"><div className="grid size-8 place-items-center rounded-md bg-primary text-primary-foreground"><Thermometer className="size-4"/></div><span className="font-semibold">ThermoShelter</span></div><h2 className="mt-7 text-2xl font-semibold">Thermal design study</h2><p className="mt-1 text-sm text-muted-foreground">{design.name} · Generated prototype report</p></div><button onClick={()=>window.print()} className="rounded-md border border-border px-4 py-2 text-sm font-medium"><Download className="mr-2 inline size-4"/>Export report</button></div><div className="grid gap-6 py-7 sm:grid-cols-2"><ReportBlock title="Project information"><Row a="Project" b={design.name}/><Row a="Status" b="Prototype / demo output"/><Row a="Generated" b="08 Sep 2026"/></ReportBlock><ReportBlock title="Climate parameters"><Row a="Location" b={climate.location}/><Row a="Climate type" b={climate.climateType}/><Row a="Ambient temperature" b={`${climate.ambientTemperature} °C`}/><Row a="Solar radiation" b={`${climate.solarRadiation} W/m²`}/></ReportBlock><ReportBlock title="Shelter parameters"><Row a="Geometry" b={`${design.length} × ${design.width} × ${design.height} m`}/><Row a="Orientation" b={design.orientation}/><Row a="Window area" b={`${design.windowAreaPercentage}%`}/></ReportBlock><ReportBlock title="Simulation results"><Row a="Avg. internal temperature" b={`${sim.averageInternalTemperature} °C`}/><Row a="Solar gain" b={`${(sim.totalSolarGain/1000).toFixed(1)} kWh`}/><Row a="Heat loss" b={`${(sim.totalHeatLoss/1000).toFixed(1)} kWh`}/></ReportBlock></div><div className="rounded-lg bg-muted p-5"><div className="text-xs font-semibold uppercase tracking-wider">Interpretation note</div><p className="mt-2 text-sm leading-6 text-muted-foreground">This report is a structured preview for the SIH prototype. Values are mock simulation output and should not be treated as experimental, ANSYS, or real-world validated results.</p></div></section></div>}
function ReportBlock({title,children}:{title:string;children:React.ReactNode}){return <div><h3 className="mb-3 text-sm font-semibold">{title}</h3><div className="flex flex-col gap-2">{children}</div></div>}
function Row({a,b}:{a:string;b:string}){return <div className="flex justify-between gap-4 border-b border-border/70 pb-2 text-xs"><span className="text-muted-foreground">{a}</span><span className="text-right font-medium">{b}</span></div>}
